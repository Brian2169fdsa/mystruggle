# CLAUDE CODE PROMPT — Second-Chance Employer Platform
# Paste into Claude Code in the existing My Struggle repo.

Read `AUTOPILOT.md`, `CLAUDE.md`, `docs/17-MODULE-EMPLOYER-PLATFORM.md`, and `requirements/14-EMPLOYER-PLATFORM.md` in full before writing code.

**EXPANSION with a strict REUSE-FIRST mandate: add ONLY the parts that don't already exist.** Your first task, before any code, is a reuse audit logged in DECISIONS.md that maps every employer feature to the existing system it wires into: member job tracking already exists (`job_applications`, docs/13 — extend with a nullable posting_id, external self-tracked applications must keep working); the résumé builder + PDF + attach-to-application already exists (docs/13); Companion job coaching, interview practice, and résumé tailoring already exist (docs/13) — point them at postings; feed promotion of jobs already exists (`sponsored_placements` kind job_opening, docs/15) — "Promote in community" hands off to it, never a duplicate ad path; hired-equals-community-capital already has rails (`continuum_events` + employment recovery goals + mv_care_outcomes, docs/13/14); staff referrals ride the existing caseload/Client 360 (docs/16); employer orgs and staff ride the existing orgs/memberships/RLS (docs/03); candidate messaging reuses the scoped moderated channel pattern (docs/14); posting review reuses the existing moderation gate (docs/15). Building a parallel application, résumé, messaging, moderation, or ad system is a P0 failure.

What IS new, build per docs/17:
- **Employer orgs + vetting**: orgs.type 'employer', employer_admin/recruiter roles, employer_profiles, the Fair-Chance Pledge acceptance, and an ms_admin verification queue — nothing an employer does is visible until verified; suspension hides everything immediately.
- **Job postings + member discovery**: job_postings with full lifecycle and moderation gate; a member job board inside Goals & Reentry with filters, saved jobs, and one-tap "Apply with my résumé" (Companion offers tailoring first); staff can refer a posting to a client, who always decides.
- **Privacy-first candidate pipeline**: kanban applied→hired; the employer sees ONLY the candidate's chosen name, résumé, and application answers — negative tests must prove no path to recovery, journey, center, continuum, or community data. Before a member's first application, a plain-language consent screen explains that applying reveals community affiliation and always offers the "export résumé and apply externally" alternative. Hired writes the continuum event, advances the employment goal, fires the consented celebration, and lands in center outcomes.
- **Employer dashboard**: Overview, Postings, Candidates, Hires with 30/90/180-day retention confirms (each confirm is a continuum event), Resources (WOTC + Federal Bonding + fair-chance guides, labeled informational-not-tax-or-legal-advice), Settings/team/pledge.
- **"For Employers" website page** (desktop + mobile): supported-candidates value story, honest employment-as-recovery-predictor framing, Fair-Chance Verified mark, how-it-works, pricing placeholder, application CTA, My Safety cross-link.
- **Compliance**: surface (never bypass) the counsel items into docs/10 §5 — pledge/EEOC language, ban-the-box variance, not-a-background-check-provider posture, disclosure consent language.

Seed per the checklist: 4 verified employers including My Safety and The Store (the docs/11 employment loop made real), 12 open postings, 2 pending verifications, 1 suspended example, and Danielle's application progressing to HIRED with the celebration, continuum event, and center outcome delta all visible.

Follow the AUTOPILOT loop: audit → rewrite GAP-REPORT.md → build highest-priority gaps (P0 = candidate privacy walls, vetting gate, reuse mandate) → verify by running → check requirements/14 boxes with evidence → re-report → commit. Full existing suite every session; any regression to job_applications/résumé/Companion/placements is a P0 stop. Keep running against `requirements/14-EMPLOYER-PLATFORM.md` until 100% complete, verified, mobile-checked, RLS-negative-tested, zero regressions, with the reuse audit proving no parallel systems exist.

Do not ask me questions mid-run; batch them into DECISIONS-NEEDED and keep building elsewhere.
