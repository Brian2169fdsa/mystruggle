# CLAUDE CODE PROMPT — "For Recovery Centers" Page + Community Ad Product
# Paste into Claude Code in the existing My Struggle repo.

Read `AUTOPILOT.md`, `CLAUDE.md`, `docs/15-MODULE-CENTERS-PAGE-AND-ADS.md`, and `requirements/12-CENTERS-PAGE-AND-ADS.md` in full before writing code. Re-read what you're extending: website docs/01, design docs/12, continuum docs/14, moderation docs/06, dashboard docs/08, and compliance docs/10.

**EXPANSION. Iron rule: expand, never replace.** Add one marketing page and one new capability (sponsored placements). Extend the moderation pipeline, dashboard, and website — do not rewrite them. Log extends in DECISIONS.md; conflicts → DECISIONS-NEEDED, keep building.

**Absolute copy rule: the word "Facebook" appears NOWHERE** — in UI, marketing copy, code, or comments. The product is "the recovery community" / "community feed." A grep for "facebook" (case-insensitive) must return zero hits; this is a checklist item.

Build per docs/15:
- **The "For Recovery Centers" marketing page** (desktop + mobile, brand system) — the page that sells centers on subscribing. Lead with the engagement argument: relapse risk falls from 40–60% in year one to under 15% (as low as 7%) after five years of continuous recovery; the first year is the highest-risk window centers currently lose visibility into; continuing care of 12+ months with active engagement produces better outcomes; over 80% of long-term relapses are preceded by an engagement drop the platform can detect; recovery-capital growth predicts retention. Render these as a cited stat spine with source tooltips and an animated relapse-risk-over-time curve as the hero proof visual, with an honest "individual results vary, recovery isn't linear" footnote. Then the three blind spots, deliver-your-programming, stay-connected-across-the-continuum, the community ad product, prove-your-outcomes, the Danielle 5-phase connect-it-all scroll, placeholder pricing tiers with a "Talk to us" CTA, a trust/privacy section, and a demo-request lead capture that writes to a dashboard lead queue + Resend notify. Voice: clinical-credible and mission-warm, never fear-mongering, never guarantees.
- **The community ad product** — sponsored_placements + placement_events with all trust rules ENFORCED IN CODE, not just documented: clearly labeled "Sponsored by [Center]" and visually distinct from peer posts; recovery-relevant categories only with hard-blocked categories (gambling/alcohol/predatory/MLM) rejected by a content-policy + Claude review gate before running; frequency-capped so the feed stays a recovery space first; targeting that is coarse and non-clinical ONLY (metro/phase/interest/circle) with health/diagnosis/substance targeting made structurally impossible; members in a crisis/at-risk state are served support resources, never ads; member dismiss/report/reduce controls; full approval workflow (draft → ms_admin review → running) audit-logged; advertiser sees aggregate analytics only, never per-member behavior.
- **Ad Manager + approval console** on the dashboard (center-side manager, ms_admin approval + content policy + frequency cap + kill switch), a subscription/billing stub, and the demo-lead queue.
- **Compliance**: surface (never bypass) the advertising-content-policy and no-health-targeting items into docs/10 §5.

Seed 2–3 approved sample placements (alumni event, fair-chance job opening, IOP program) running in Danielle's feed under the frequency cap, one pending in the ms_admin queue, one rejected-with-reason, aggregate ad analytics, and demo-request leads — so every new surface looks alive.

Follow the AUTOPILOT loop: audit → rewrite GAP-REPORT.md → build highest-priority gaps (P0 = the ad trust guarantees: no health targeting, crisis-state members never served ads, every placement moderated) → verify each by running it → check requirements/12 boxes with evidence → re-report → commit. Run the full existing suite every session; any regression to website/feed/moderation/continuum/dashboard is a P0 stop, and the feed must still visibly feel like a recovery space (frequency cap holds in seed). Keep running against `requirements/12-CENTERS-PAGE-AND-ADS.md` until 100% complete, verified, mobile-checked, RLS- and targeting-negative-tested, "Facebook" grep clean, zero regressions.

Do not ask me questions mid-run; batch them into DECISIONS-NEEDED and keep building elsewhere.
