# CLAUDE CODE PROMPT — Center Operations Suite (dashboard deep build + client portal + care team + ROI)
# Paste into Claude Code in the existing My Struggle repo.

Read `AUTOPILOT.md`, `CLAUDE.md`, `docs/16-MODULE-CENTER-OPERATIONS.md`, and `requirements/13-CENTER-OPERATIONS.md` in full before writing code. Re-read what you're extending: LMS docs/07, dashboard docs/08, community expansion docs/13, continuum docs/14 (care episodes, channels, continuum_events), centers page docs/15, and requirements/02 (member app).

**EXPANSION. Iron rule: expand, never replace.** Programs sit ABOVE existing courses and reuse them as ingredients — do not fork the course system. The client portal is an additive "In Treatment" mode of the EXISTING member PWA (a My Program panel + policy toggles), not a second app. Care-team messaging reuses the existing care channels. Log every extend in DECISIONS.md; conflicts → DECISIONS-NEEDED, keep building.

Build per docs/16:
- **Program Builder** — centers compose runnable Programs (curriculum of existing courses + auto-scheduled session series + reentry task packs + milestones + gamification config), publish, and enroll cohorts; publishing auto-creates the program group channel and cockpit; seed 4 My Struggle starter templates (ISE 12-Step, IOP Core, Vocational Readiness, Reentry Navigation) with clone-and-customize.
- **Client 360** — one screen per person showing before/during/after: continuum ribbon with consented pre-care baseline, and tabs for Engagement, Learning, Community (audience-legitimate content ONLY — never private journals or mentor DMs), Goals & Reentry with recovery-capital deltas, Giving (permission-gated), and Care & Support. Staff-only cohort compare that is never member-facing. Under 2 seconds on seed.
- **Care Team & Staff Engagement** — assign case managers/counselors/peer support/techs per episode; My Caseload sorted by risk and last-touch with "untouched 3 days" surfacing; kudos/nudges/check-ins/1:1 messages as one-tap actions; EVERY staff touch writes staff_engagements + a continuum_event so human contact becomes measurable; staff task queue; no-PHI rule enforced in every touch UI.
- **Client Portal (In-Treatment mode)** — My Program panel atop the existing Home: Today view (sessions/assignments/tasks), curriculum progress, cohort channel, Kudos inbox, and My Reentry Plan where clients create their own goals and tasks with Companion coaching; kiosk mode (PIN/QR quick-login, session-scoped, auto-logout) for facility devices; per-org policy toggles (community access by phase, quiet hours, portal-only) enforced in code; gamification identical inside and out.
- **Engagement Toolkit** — cohort challenges (opt-in, badge, no loser-boards), events with RSVP→attendance→continuum_event, pulse surveys (anonymous-to-peers, staff trends), announcement read receipts, milestone spotlights with client consent prompt.
- **ROI & Executive Reporting** — mv_roi_inputs; center_admin ROI dashboard (their assumptions × platform-measured actuals, math shown transparently, New Freedom benchmarks labeled as case-study reference); staff-touch→outcome correlation view; branded executive one-pager PDF with the Claude narrative.
- **Marketing page update (docs/15 page)** — add the four showcase sections (Build your programming / See the whole person / Every employee, engaged / A portal your clients actually use) and the interactive ROI calculator with sliders and an honest footnote feeding the demo-lead CTA. Keep every existing section, citations, and the zero-"Facebook" grep.
- **Dashboard IA** — final left nav (Overview · Programs · Participants · Caseloads · Mentorship · Community · Giving · Engage · Reports · Settings) with correct role gating and nothing orphaned.

Seed richly per the checklist: Desert Hope running 3 programs with cohorts/sessions/attendance; Danielle in IOP Core with a care team of 3, kudos and check-in history, a challenge, pulse responses, and a fully alive Client 360 including her pre-care baseline; ROI dashboard, one-pager, and the public calculator all producing sane numbers from seed.

Follow the AUTOPILOT loop: audit → rewrite GAP-REPORT.md → build highest-priority gaps (P0 = privacy walls in Client 360, no-PHI enforcement, portal policy toggles) → verify by running → check requirements/13 boxes with evidence → re-report → commit. Full existing suite every session; any regression to LMS/dashboard/member app/continuum is a P0 stop; anti-toxicity and privacy rules re-verified every run. Keep running against `requirements/13-CENTER-OPERATIONS.md` until 100% complete, verified, mobile- and kiosk-checked, RLS-negative-tested, zero regressions.

Do not ask me questions mid-run; batch them into DECISIONS-NEEDED and keep building elsewhere.
